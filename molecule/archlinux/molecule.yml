---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
lint:
  name: yamllint
driver:
  name: docker
platforms:
  - name: lynis-archlinux
    image: archlinux/base
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: archlinux
